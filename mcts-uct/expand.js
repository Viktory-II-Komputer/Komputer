
export function expand(parent)
{
    // Will need some hook into the game being played.
    // Create all possible child nodes for given parent.
    // Each child.isPlayer1 should be constructed with the opposite of parent.isPlayer1.

}